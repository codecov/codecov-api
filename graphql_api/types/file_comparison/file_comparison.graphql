type FileComparison {
    baseName: String
    headName: String
    isNewFile: Boolean
    baseTotals: CoverageTotals
    headTotals: CoverageTotals
    hasDiff: Boolean
    lines: [LineComparison!]!
}
