scalar DateTime

type Query {
  me: Me
  owner(username: String!): Owner
  loginProviders: [LoginProvider!]! @deprecated(reason: "Use `settings.loginProviders`")
  settings: Settings!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
