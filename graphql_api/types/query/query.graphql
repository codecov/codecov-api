scalar DateTime

type Query {
  me: Me
  owner(username: String!): Owner
  loginProviders: [LoginProvider!]! @deprecated(reason: "Use `config.loginProviders`")
  config: Config!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
