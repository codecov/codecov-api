type Repository {
  name: String!
  active: Boolean!
  private: Boolean!
  coverage: Float
  latestCommitAt: DateTime
  updatedAt: DateTime
  author: Owner!
  branch:(name: String!)
  commit(id: String!): Commit
}
