type Pull {
  behind_by: Int
  behind_by_commit: String
  title: String
  state: PullRequestState
  pullId: Int
  author: Owner
  updatestamp: DateTime
  head: Commit
  comparedTo: Commit
  compareWithBase: ComparisonResult
  compareWithBaseTemp: Comparison
  commits(
    first: Int
    after: String
    last: Int
    before: String
  ): CommitConnection
}
