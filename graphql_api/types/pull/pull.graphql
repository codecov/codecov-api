type Pull {
  title: String
  state: PullRequestState
  pullId: Int
  author: Owner
  updatestamp: DateTime
  head: Commit
  comparedTo: Commit
  compareWithBase: Comparison
  commits(first: Int, after: String, last: Int, before: String): CommitConnection
  pullComparison: PullComparison
}
