input MyOrganizationFilters {
  term: String
}

input ViewableRepositoryFilters {
  term: String
  active: Boolean
}

type Me {
  user: User!
  owner: Owner!
  viewableRepositories(filters: ViewableRepositoryFilters, first: Int, after: String, last: Int, before: String): ViewableRepositoryConnection!
  myOrganizations(filters: MyOrganizationFilters, first: Int, after: String, last: Int, before: String): MyOrganizationConnection!
}
